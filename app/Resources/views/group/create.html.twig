{% extends 'base.html.twig' %}

{% block body %}
	<div class="card">
      <h5 class="card-header">Add Group</h5>
      <div class="card-body">
  		{{ form_start(form) }}
  		{{ form_widget(form) }}
  		{{ form_end(form) }}
      </div>
    </div>
{% endblock %}
